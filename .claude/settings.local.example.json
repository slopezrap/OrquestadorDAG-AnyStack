{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Copy this file to settings.local.json and add personal overrides only. Repo-wide policy lives in settings.json — do NOT duplicate it here.",
  "permissions": {
    "defaultMode": "bypassPermissions"
  }
}
